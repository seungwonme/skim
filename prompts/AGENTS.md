# prompts

Prompt/session archive snapshots.

## Rules

- Files are records, not runtime inputs.
- Preserve `YYYY-MM-DD.json` naming for new snapshots.
- Do not import these files from app, CLI, core, or tests.
- Prefer archival or compression over deletion when cleaning old snapshots.
